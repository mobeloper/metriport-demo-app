import { Box, Heading, VStack, Flex, Image } from "@chakra-ui/react";

export default function Home({}: {}) {
  return (
    <Box>
      <VStack align="stretch">
        <Heading>Welcome!</Heading>
      </VStack>
    </Box>
  );
}
